class Solution(object):
    def isPalindrome(self, x):
        c=x
        a=0
        while x>0:
           last=x%10
           x/=10
           a=(a*10)+last
        if a==c:
         return True
        else:
         return False
